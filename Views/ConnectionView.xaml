<UserControl x:Class="DeskAppWPF.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeskAppWPF.Views"
             xmlns:viewmodels="clr-namespace:DeskAppWPF.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ConnectionViewModel}">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
        <Ellipse Width="12" Height="12" Fill="{Binding ConnectionColor}" Margin="0,0,8,0" VerticalAlignment="Center"/>
        <TextBlock Text="{Binding ConnectionText}" FontWeight="SemiBold" Foreground="#888888" VerticalAlignment="Center" FontSize="14"/>
    </StackPanel>
</UserControl>

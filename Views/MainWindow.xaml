<Window x:Class="DeskAppWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
    Title="Main Window" Height="450" Width="800"
        Closing="Window_Closing">
    
    <Grid>
        <tb:TaskbarIcon
            x:Name="TrayIcon"
            ToolTipText="Desk Automation App"
            DoubleClickCommand="{Binding ShowWindowCommand, RelativeSource={RelativeSource AncestorType=Window}}">
            <tb:TaskbarIcon.IconSource>
                <tb:GeneratedIconSource
            Text="1"
            Foreground="Red"
            />
            </tb:TaskbarIcon.IconSource>
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Open" Click="MenuOpen_Click" />
                    <MenuItem Header="Exit" Click="MenuExit_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
        
        <ContentControl Content="{Binding CurrentPage}" />
    </Grid>
</Window>

<UserControl x:Class="DeskAppWPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeskAppWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,20">
            <Button Content="&#x2190; Back" Command="{Binding NavigateBackCommand}" 
                    Background="Transparent" BorderBrush="Transparent" Foreground="#007ACC"
                    FontSize="16" FontWeight="SemiBold" Margin="0,0,15,0" VerticalAlignment="Center" Cursor="Hand"/>
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="ICS Link for Calendar:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding IcsLink, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Desk IP Address:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding DeskIpAddress, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <TextBlock Text="Buffer Zone (Minutes before event):" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding BufferZoneMinutes, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,0,0,20">
            <CheckBox Content="Run automation even when PC is locked" IsChecked="{Binding RunWhenPcLocked, UpdateSourceTrigger=PropertyChanged}" FontWeight="SemiBold"/>
        </StackPanel>

        <Button Grid.Row="5" Content="Save Settings" Command="{Binding SaveCommand}" 
                HorizontalAlignment="Left" Padding="15,5" Background="#007ACC" Foreground="White" BorderBrush="Transparent"/>
    </Grid>
</UserControl>
